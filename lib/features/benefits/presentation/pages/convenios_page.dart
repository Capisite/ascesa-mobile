import 'package:flutter/material.dart';

class ConveniosPage extends StatelessWidget {
  const ConveniosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Convênios Page'));
  }
}
